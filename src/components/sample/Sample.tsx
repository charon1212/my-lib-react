import { Button, Typography } from '@mui/material';

export const Sample = () => {
  return (
    <>
      <Typography>Sample Component</Typography>
      <Button onClick={() => alert('sample')}>SAMPLE</Button>
    </>
  );
};
